## 2026-04-24 - US-047
- Created `src/routes/_authed/bank/index.tsx` — bank reconciliation UI page
  - Client selector dropdown at top
  - Bank account selector per selected client
  - "Nueva cuenta" button + inline form to create bank accounts (bankName, accountNumber, alias, CBU)
  - Summary stats cards: account count, total transactions, matched, pending, match rate, total credit/debit
  - Transaction list with per-row matched/unmatched indicator (CheckCircle2 vs CircleDashed)
  - Match type badge (Auto/Manual) with confidence percentage
  - Color-coded amounts (credit=green, debit=red)
  - "Auto-conciliar" button triggers autoMatchTransactions and shows matched count in toast
- Added `Landmark` icon import and `<NavItem to="/bank" icon={Landmark} label="Banco" />` to `src/components/app-sidebar.tsx`
- Regenerated `src/routeTree.gen.ts` to include the new `/_authed/bank/` route
- Files changed: `src/routes/_authed/bank/index.tsx` (new), `src/components/app-sidebar.tsx`, `src/routeTree.gen.ts`
- **How to verify:**
  - Run `bun run dev`, navigate to `/bank`
  - "Banco" appears in sidebar with Landmark icon
  - Select a client → summary stats appear + account selector populates
  - Click "Nueva cuenta" → inline form appears → enter banco name → save → account appears in dropdown
  - Select an account → transaction list appears (empty if no transactions imported)
  - If transactions exist: matched ones show green CheckCircle2, unmatched show gray CircleDashed
  - "Auto-conciliar (N)" button appears when account is selected; clicking it triggers auto-matching
- **Learnings for future iterations:**
  - TanStack Router route tree must be regenerated after adding new routes: run `bunx tsr generate` or `bunx @tanstack/router-cli generate`
  - `void queryClient.invalidateQueries(...)` is required to satisfy the `no-floating-promises` ESLint rule — always add `void` prefix
  - Pre-existing `BarChart2` unused import in app-sidebar.tsx is a known non-regression error
---

## 2026-04-24 - US-046
- Created `src/actions/bank.tsx` with all 7 bank reconciliation server functions:
  - `createBankAccount`: POST, requires canWrite, validates client belongs to org, inserts bank account
  - `listBankAccounts(clientId)`: GET, validates client belongs to org, returns active accounts ordered by createdAt
  - `importBankTransactions(bankAccountId, transactions[])`: POST, requires canWrite, deduplicates by externalId, bulk inserts transactions
  - `listBankTransactions(bankAccountId, from?, to?)`: GET, returns transactions with their match status (joined from bankInvoiceMatch)
  - `autoMatchTransactions(bankAccountId)`: POST, requires canWrite; matches unmatched transactions to invoices by amount (< 1 peso tolerance) + date proximity (5 days) + counterparty CUIT (bonus confidence); inserts auto matches with confidence score
  - `manualMatchTransaction(transactionId, invoiceId)`: POST, requires canWrite; removes existing match, inserts manual match with confidence 100.00 and reviewedByUserId
  - `getReconciliationSummary(clientId)`: GET; returns totalTransactions, matchedTransactions, unmatchedTransactions, matchRate, totalCredit, totalDebit, accountCount
- All functions validate org ownership via `ensureBankAccountBelongsToOrg` helper (JOIN bankAccount → client → organizationId)
- Auto-matching confidence: 50 base (amount + date match) + 40 for CUIT match + up to 10 for date proximity bonus
- Files changed: `src/actions/bank.tsx` (new)
- **How to verify:**
  - Run `bun run dev`
  - Call `createBankAccount({ clientId: '<uuid>', bankName: 'Banco Nación', currency: 'ARS' })` — inserts into bank_account
  - Call `listBankAccounts({ clientId: '<uuid>' })` — returns list of active accounts
  - Call `importBankTransactions({ bankAccountId: '<uuid>', transactions: [{ transactionDate: '2026-04-01', amount: '1000.00', direction: 'credit' }] })` — inserts transaction, returns `{ imported: 1, skipped: 0 }`
  - Call `autoMatchTransactions({ bankAccountId: '<uuid>' })` — matches transactions to invoices, returns `{ matched: N }`
  - Call `getReconciliationSummary({ clientId: '<uuid>' })` — returns counts and totals
- **Learnings for future iterations:**
  - Zod v4 (used in this project) requires 2 args for `z.record()`: `z.record(z.string(), z.unknown())` — not `z.record(z.unknown())`
  - Drizzle tables don't have `.as()` for aliases — use `innerJoin` directly or raw SQL `sql\`...\`` for complex aliased queries
  - `inArray()` with a nullable column (`text nullable`) works fine in Drizzle — no casting needed
  - Auto-matching with confidence score: amount tolerance < 1 unit is a practical threshold for Argentine peso amounts
---

## 2026-04-24 - US-045
- Added 4 bank reconciliation tables to `drizzle/schema.ts`:
  - `bankAccount`: per-client bank accounts with bank_name, account_number, currency, alias, cbu, active
  - `bankTransaction`: transactions per bank account with transaction_date, amount, direction, counterparty info, external_id, raw_data
  - `bankInvoiceMatch`: matches between bank transactions and invoices with match_type, confidence, reviewed_by_user_id
  - `financialMovementClassification`: movement classification with source_type/source_id, category, is_business_related, is_tax_relevant, is_cashflow_real, classified_by
- Created `src/scripts/ensure-bank-reconciliation-tables.ts` migration script (idempotent, uses CREATE TABLE IF NOT EXISTS for all 4 tables)
- Ran migration script — all 4 tables OK (exit 124 expected)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-bank-reconciliation-tables.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d bank_account'` to confirm table with all columns
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\dt bank_%'` to see all 3 bank tables
  - Or: `timeout 60 bun run src/scripts/ensure-bank-reconciliation-tables.ts` — prints OK for each table
- **Learnings for future iterations:**
  - `bankInvoiceMatch` references `invoice` table — verified it exists as `export const invoice` in schema.ts
  - All 4 tables can be created in a single migration script, each with its own `db.execute(sql.raw(...))` call for clarity
  - Pre-existing typecheck errors are only in legacy scripts (map-import-empleados-a-convenios.ts, report-unmatched-import-empleados.ts, etc.) — not regressions
---

## 2026-04-24 - US-044
- Added `payrollReceiptTemplate` import and 3 new server functions to `src/actions/sueldos.ts`:
  - `createReceiptTemplate(profileId, name, receiptType, conceptIds)`: POST, requires canWrite, validates profile belongs to org, inserts template with jsonb conceptIds
  - `listReceiptTemplates(profileId)`: GET, validates profile belongs to org, returns active templates ordered by createdAt
  - `deleteReceiptTemplate(id)`: POST, requires canWrite, fetches template to get profileId, validates org ownership, deletes
- Added private `ensureProfileBelongsToOrg(profileId, orgId)` helper — joins `profile → client` and checks `client.organizationId = orgId`
- Updated `src/components/sueldos/SueldosSimulador.tsx`:
  - Added `listReceiptTemplates`, `createReceiptTemplate`, `deleteReceiptTemplate` imports
  - Added `ReceiptTemplate` interface and `useQuery` for template list (keyed on profileId)
  - "Usar template" dropdown appears when templates exist in manual mode — filters concept list to template's concept codes
  - Delete (trash) button removes selected template and resets selection
  - "Guardar como template" button opens a dialog to name the template; saves current visible concepts as conceptIds
  - `saveTemplate` mutation calls `createReceiptTemplate` and invalidates `receipt-templates` query
  - `removeTemplate` mutation calls `deleteReceiptTemplate` and invalidates query
- Files changed: `src/actions/sueldos.ts`, `src/components/sueldos/SueldosSimulador.tsx`
- **How to verify:**
  - Run `bun run dev`, navigate to Sueldos → select a client/profile → Simulador tab
  - Choose "Carga manual" option and submit the form to reach the concepts table
  - "Guardar como template" button appears at bottom-right; click it → dialog opens asking for template name → save
  - Template now appears in "Usar template…" dropdown; selecting it filters the concepts table to only the saved codes
  - Trash button (red) next to the dropdown removes the selected template
  - Verify via DB: `docker exec arca-local-db psql -U arca -d arca -c 'SELECT * FROM payroll_receipt_template LIMIT 5'`
- **Learnings for future iterations:**
  - `payrollReceiptTemplate` has `conceptIds jsonb` — store as `string[]` (concept codes), return `as any` to avoid TS jsonb `unknown` type issues (same pattern as `employeeEvent`, `credential.tsx`)
  - For profile-scoped templates, validate org ownership by joining `profile → client` via `profile.client` (not `profile.clientId`) and checking `client.organizationId = orgId`
  - `useQuery<ReceiptTemplate[]>` with explicit type parameter + `as Promise<ReceiptTemplate[]>` cast is the pattern when server fn returns `any`
  - The "Usar template" dropdown uses `Select` from shadcn — import from `@/components/ui/select`
---

## 2026-04-24 - US-043
- Added `payrollPeriodNovelty` import to `src/actions/sueldos.ts`
- Implemented `createNovelty`: POST, requires canWrite, validates empleado belongs to org, inserts row with empleadoId, periodo, type, quantity, amount, description
- Implemented `listNovelties`: GET, validates empleado belongs to org, returns novelties filtered by empleadoId + periodo ordered by createdAt asc
- Implemented `deleteNovelty`: POST, requires canWrite, fetches novelty to get empleadoId, validates org ownership via `ensureEmpleadoBelongsToOrg`, then deletes
- Numeric values (quantity, amount) are stored as strings via `String()` to satisfy Drizzle's `numeric` column type
- Files changed: `src/actions/sueldos.ts`
- **How to verify:**
  - Run `bun run dev`
  - Call `createNovelty({ empleadoId: '<uuid>', periodo: '2026-04', type: 'ausencia', quantity: 1 })` — inserts a row in `payroll_period_novelty`
  - Call `listNovelties({ empleadoId: '<uuid>', periodo: '2026-04' })` — returns array of novelties for that period
  - Call `deleteNovelty({ id: '<uuid>' })` — removes the row; calling with a novelty from another org throws "Empleado no encontrado o no autorizado"
  - Verify in DB: `docker exec arca-local-db psql -U arca -d arca -c 'SELECT * FROM payroll_period_novelty LIMIT 5'`
- **Learnings for future iterations:**
  - Drizzle `numeric` columns expect `string | null`, not `number | null` — use `String(value)` when inserting numeric JS values
  - `deleteNovelty` needs a two-step approach: first fetch the novelty to get `empleadoId`, then validate org ownership, then delete — cannot short-circuit without fetching first
  - `ensureEmpleadoBelongsToOrg` is the reusable org-scoping helper for all operations touching `liquidacionImportEmpleado`-derived data
---

## 2026-04-24 - US-042
- Added expandable timeline section per employee in `src/components/sueldos/SueldosEmpleados.tsx`
- Added `EmpleadoTimeline` sub-component: fetches events via `listEmployeeEvents`, shows vertical timeline with color-coded type badges, date, title, description, and "Afecta liquidación" indicator
- Added `EVENT_TYPES` constant and `EVENT_TYPE_COLORS` map (9 types with Arca color tokens)
- Each employee row now has a ChevronRight/ChevronDown toggle button in a new column; click expands the timeline below
- "Agregar evento" button (within timeline) opens a dialog with: type selector, date picker, title input, description textarea, affects_payroll Switch toggle
- `crearEvento` mutation calls `createEmployeeEvent`, then invalidates the per-employee events query
- Files changed: `src/components/sueldos/SueldosEmpleados.tsx`
- **How to verify:**
  - Run `bun run dev`, navigate to Sueldos → select a client/profile → Empleados tab
  - Each row now has a ChevronRight icon button on the rightmost column
  - Click it: a timeline section expands below that employee (shows "Sin eventos registrados." if none)
  - Click "Agregar evento": dialog opens with type selector, date picker, title, description, and "Afecta liquidación" toggle
  - Fill in and click "Guardar" → event appears in the timeline with color-coded badge
  - Re-collapse and re-expand: events persist (fetched from DB)
- **Learnings for future iterations:**
  - React.Fragment with key prop is required when returning multiple sibling `<tr>` elements from a map — `<>...</>` shorthand doesn't accept a key
  - `EmpleadoTimeline` is a sub-component that makes its own query per employee — only fires when the row is expanded (lazy loading pattern)
  - `colSpan={11}` needed for the expanded row since we now have 11 columns (added one for expand toggle)
  - `useQuery` inside a sub-component with `queryKey: ['employee-events', empleadoId]` ensures proper cache isolation per employee
---

## 2026-04-24 - US-041
- Added `createEmployeeEvent` and `listEmployeeEvents` server functions to `src/actions/sueldos.ts`
- Added `employeeEvent` to schema imports in sueldos.ts
- Added `ensureEmpleadoBelongsToOrg(empleadoId, orgId)` private helper: joins `liquidacionImportEmpleado` → `profile` → `client` and checks `client.organizationId = orgId`
- `createEmployeeEvent`: POST, requires canWrite, validates empleado belongs to org, inserts into `employee_event`, sets `createdByUserId` from session
- `listEmployeeEvents`: GET, validates empleado belongs to org, returns events ordered by `eventDate` desc, limit defaults to 50
- Both return `as any` to avoid jsonb `metadata: unknown` vs `{}` type mismatch (same pattern as credential.tsx, agent.tsx)
- Files changed: `src/actions/sueldos.ts`
- **How to verify:**
  - Run `bun run dev`
  - Call `createEmployeeEvent({ empleadoId: '<uuid>', type: 'ausencia', title: 'Ausencia por enfermedad', eventDate: '2026-04-01' })` — inserts a row in `employee_event`
  - Call `listEmployeeEvents({ empleadoId: '<uuid>' })` — returns array of events sorted by date desc
  - Calling with an empleado from a different org throws "Empleado no encontrado o no autorizado"
- **Learnings for future iterations:**
  - `ensureEmpleadoBelongsToOrg` validates via JOIN chain: `liquidacionImportEmpleado` → `profile` (via `profileId`) → `client` (via `profile.client`) → check `client.organizationId = orgId`
  - `liquidacionImportEmpleado.profileId` uses `.profileId` naming (not `.profile`) — different from the `.client` pattern used on older tables
  - Return `as any` for functions that include jsonb columns in their return type
---

## 2026-04-24 - US-039
- Added `payrollReceiptTemplate` table to `drizzle/schema.ts` with all required columns: id, profileId (FK CASCADE to profile), name, receiptType (default 'sueldo'), conceptIds (jsonb), active (bool default true), createdAt
- Created `src/scripts/ensure-payroll-receipt-template-table.ts` migration script (idempotent, uses CREATE TABLE IF NOT EXISTS)
- Ran migration script — output: "Tabla payroll_receipt_template: OK." (exit 124 expected)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-payroll-receipt-template-table.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d payroll_receipt_template'` to confirm table with all columns
  - Or: `timeout 60 bun run src/scripts/ensure-payroll-receipt-template-table.ts` — prints "Tabla payroll_receipt_template: OK."
- **Learnings for future iterations:**
  - All pre-existing typecheck errors are in legacy scripts — not regressions
---

## 2026-04-24 - US-038
- Added `payrollPeriodNovelty` table to `drizzle/schema.ts` with all required columns: id, empleadoId (FK CASCADE to liquidacion_import_empleado), periodo, type, quantity (numeric 10,2 nullable), amount (numeric 14,2 nullable), description, appliedToReciboId (FK SET NULL to liquidacion_import_recibo nullable), status (default 'pending'), createdAt
- Created `src/scripts/ensure-payroll-period-novelty-table.ts` migration script (idempotent, uses CREATE TABLE IF NOT EXISTS)
- Ran migration script — output: "Tabla payroll_period_novelty: OK." (timeout exit code 124 expected)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-payroll-period-novelty-table.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d payroll_period_novelty'` to confirm table with all columns
  - Or: `timeout 60 bun run src/scripts/ensure-payroll-period-novelty-table.ts` — prints "Tabla payroll_period_novelty: OK."
- **Learnings for future iterations:**
  - `appliedToReciboId` references `liquidacion_import_recibo` — SQL table name is `liquidacion_import_recibo` (underscore prefix pattern)
  - All pre-existing typecheck errors (drizzle/relations.ts, credential.tsx, sueldos.ts, dashboard.tsx) are unrelated to our changes
---

## 2026-04-24 - US-037
- Added `employeeEvent` table to `drizzle/schema.ts` with all required columns: id, empleadoId (FK CASCADE to liquidacion_import_empleado), type, title, description, eventDate, affectsPayroll (default false), metadata (jsonb), createdByUserId (FK nullable to user), createdAt
- Created `src/scripts/ensure-employee-event-table.ts` migration script (idempotent, uses CREATE TABLE IF NOT EXISTS)
- Ran migration script — output: "Tabla employee_event: OK." (timeout exit code 124 is expected with DB connections not cleanly closing in bun)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-employee-event-table.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d employee_event'` to confirm table exists with all columns
  - Or run `timeout 60 bun run src/scripts/ensure-employee-event-table.ts` — should print "Tabla employee_event: OK."
- **Learnings for future iterations:**
  - Pre-existing typecheck has 98 errors in legacy scripts (map-import-empleados-a-convenios.ts, report-unmatched-import-empleados.ts, etc.) — these are not regressions from our changes
  - Migration script exit code 124 (timeout) is expected — the DB connection doesn't cleanly close; if output says "OK" the migration succeeded
---

## 2026-04-24 - US-036
- Backend: `uploadDocumentForRequest` and `getRequestDocument` were already implemented in `src/actions/client-portal.tsx`
  - `uploadDocumentForRequest`: validates user has `canUploadDocuments` access, reads file as base64 data URL, inserts into `document` table with `storageProvider='upload'`, updates `clientRequest.metadata` with `{ documentId, documentName, documentMimeType, uploadedAt, uploadedByUserId }`
  - `getRequestDocument`: studio-side, validates org ownership, fetches document by `metadata.documentId`
- Portal UI (`src/routes/_client/portal/solicitudes/index.tsx`):
  - Added `PortalRequest` typed interface with `metadata?: { documentId?, documentName? }`
  - Added `uploadMutation` that reads file via `FileReader.readAsDataURL`, strips prefix, calls `uploadDocumentForRequest`
  - Hidden `<input type="file">` triggered by "Subir documento" button per request
  - For `type === 'document'` open requests: shows "Subir documento" button or "✓ Documento enviado" when already uploaded
  - For other open requests: shows "Completar" button
  - Uploaded doc name shown as a badge in the request card
- Studio UI (`src/components/client-detail-page.tsx`):
  - Added `metadata?: { documentId?, documentName? }` to `RequestRow` interface
  - Added `Paperclip` icon in title cell when request has attached document
  - Added document file name subtitle below title when document is attached
  - Added `getRequestDocument` import and "Doc" download button in actions column
  - Download: fetches doc on click, creates `<a>` element with data URL href and `download` attribute
- Files changed: `src/routes/_client/portal/solicitudes/index.tsx`, `src/components/client-detail-page.tsx`, `src/actions/client-portal.tsx`
- **How to verify:**
  - Run `bun run dev`
  - As a portal user: go to `/portal/solicitudes` — find a request with `type=document` and status `open`
  - Click "Subir documento" → file picker opens → select any file → "Documento enviado" indicator appears
  - Reload: document badge persists (stored in DB via metadata)
  - As a studio user: go to client detail → Solicitudes tab → request that has a document shows Paperclip icon + filename + "Doc" download link
  - Click "Doc" → file downloads to browser
- **Learnings for future iterations:**
  - `FileReader.readAsDataURL` returns `data:mime;base64,DATA` — strip prefix with `result.split(',')[1]` to get raw base64
  - `document.url` stores the full data URL; creating `<a href={url} download={name}>` and calling `.click()` triggers download without a server round-trip
  - `useQuery` generic inference doesn't narrow `data` type even with a typed default — use `const typed: Type[] = (raw as Type[]) ?? []` pattern after the query
  - `metadata` in `RequestRow` must be explicitly typed in the interface (not just `unknown`) for accessing nested fields without `as any`
---

## 2026-04-24 - US-035
- Added studio-side server functions to `src/actions/client-portal.tsx`: `listClientRequests`, `createClientRequest`, `updateClientRequestStatus`
  - All use `getSessionWithOrg()` + validate client belongs to org; `createClientRequest` and `updateClientRequestStatus` also call `assertCanWrite(role)`
  - Explicit column selects (no `metadata`) to avoid `unknown` vs `{}` type conflicts with jsonb
- Added "Solicitudes" tab to `src/components/client-detail-page.tsx`
  - Tab trigger with ClipboardList icon added to the tab bar
  - Tab content: status filter dropdown, "Nueva solicitud" button, requests table with status badges, Completar/Cancelar/Reabrir action buttons
  - State: `solicitudesStatusFilter`, `newRequestDialogOpen`, form fields for new request (title, description, type, dueAt)
  - `listClientRequests` query + `createClientRequest` and `updateClientRequestStatus` mutations wired in
  - Dialog for creating new requests with title, description, type selector, optional due date
  - Type `RequestRow` defined inline to work around TanStack Query's `unknown` inference with jsonb
- Created `src/routes/_client/portal/solicitudes/index.tsx` — portal-facing requests page
  - Filter bar (Pendientes / Completadas / Todas), list of requests with status badges, type, due date, completed date
  - "Completar" button per open request calls `completeClientRequest`, invalidates portal dashboard query
- Files changed: `src/actions/client-portal.tsx`, `src/components/client-detail-page.tsx`, `src/routes/_client/portal/solicitudes/index.tsx` (new), `src/routeTree.gen.ts`
- **How to verify:**
  - Run `bun run dev`
  - Navigate to any client detail page (`/clients/$clientId`) → new "Solicitudes" tab appears in tab bar
  - In Solicitudes tab: click "Nueva solicitud" → dialog opens; fill title + type + optional due date → click "Crear solicitud" → row appears in the table
  - Row shows status badge "Abierta"; click "Completar" → status updates to "Completada"; click "Reabrir" → back to "Abierta"
  - As a portal user at `/portal/solicitudes`: pending requests appear; click "Completar" → request disappears from Pendientes filter
- **Learnings for future iterations:**
  - jsonb columns in Drizzle return `metadata: unknown`; this causes TanStack server function type errors when using `select()` without specifying columns — use explicit column selects to exclude jsonb fields
  - `useQuery` data types as `unknown` when server function return contains `unknown` — define a local `type RequestRow = {...}` and cast the default: `= [] as RequestRow[]`
  - `updateClientRequestStatus` sets `completedAt: null` when status is not 'completed', preserving correctness for reopen/cancel flows
---

## 2026-04-24 - US-034
- Created `src/routes/_client/portal/index.tsx` — client portal dashboard page at `/portal`
- Shows: greeting with client name, 4 summary cards (vencimientos, deudas, notificaciones, solicitudes), next 3 due dates panel, open debts panel, pending requests panel, unread notifications count panel
- Uses Arca design tokens: `--arca-accent-primary`, `--arca-accent-neg`, `--arca-accent-pos`, `--arca-accent-warn`, `--arca-surface`, `--arca-border`, `--arca-shadow-sm`, `--arca-ink`, `--arca-ink-3`
- Gets `clientId` from parent route context via `Route.useRouteContext() as { clientId: string; access: ... }`
- Calls `getClientPortalDashboard({ data: { clientId } })` with `staleTime: 60_000`
- Date values from server functions must be cast with `as unknown as string` before passing to `new Date()` (server returns `Date` type but wire-serializes to string)
- Also updated `src/routeTree.gen.ts` via `bunx @tanstack/router-cli generate` — CLI now succeeds because `_client` has a child route; auto-picked up `alerts` route from US-029 too
- Files changed: `src/routes/_client/portal/index.tsx` (new), `src/routeTree.gen.ts`
- **How to verify:**
  - Run `bun run dev`
  - Insert a `client_user_access` row for your user: `INSERT INTO client_user_access (id, client_id, user_id) VALUES (gen_random_uuid(), '<clientUuid>', '<userId>')`
  - Navigate to `/portal` — the simplified sidebar appears with "Portal del Cliente" header
  - Dashboard shows: greeting with client name, summary cards row, next 3 due dates card, open debts card, pending requests card (if any)
  - If `canViewDebts=true` on the access row, debts card appears; otherwise it's hidden
  - Summary cards show green/amber/red colors based on counts
- **Learnings for future iterations:**
  - `bunx @tanstack/router-cli generate` succeeds once `_client` has at least one child route — run it before manually editing routeTree.gen.ts
  - Child route uses `Route.useRouteContext() as { clientId: string }` to access parent `beforeLoad` return value
  - Date fields from Drizzle server functions come typed as `Date` but wire-serialized as strings — cast with `as unknown as string` when passing to `new Date()`
  - `_client/portal/index.tsx` is the actual file path (not `_client/index.tsx`) because the sidebar links use `/portal` prefix, not the root
---

## 2026-04-24 - US-033
- Created `src/routes/_client/route.tsx` — pathless layout for client portal users
  - `beforeLoad` calls `getClientPortalSession()` and redirects to `/login` if user has no `clientUserAccess` row
  - Returns `{ clientId, access }` as route context for child routes
  - Layout renders a simplified sidebar: Home, Vencimientos, Deudas, Notificaciones, Solicitudes (NO admin, sueldos, jobs)
  - Sidebar has logout button using `authClient.signOut()`
  - NavItems link to `/portal/*` prefix to avoid URL conflicts with `_authed` routes
- Added `getClientPortalSession()` to `src/actions/helpers.ts`
  - Calls `getAuthSession()`, queries `clientUserAccess` for the user
  - Returns `{ session, userId, clientId, access }` — throws `'Sin acceso al portal del cliente'` if no access row
  - Added `clientUserAccess` to the imports from `@/drizzle/schema`
- Manually updated `src/routeTree.gen.ts` to register `/_client` route
  - Added import, route constant, `FileRoutesById` entry, `FileRouteTypes.id` union, `FileRoutesByPath` module augmentation, `RootRouteChildren` interface and constant entries
  - Required because `bunx @tanstack/router-cli generate` fails when `_client` has no children yet (conflicts with `_authed/index.tsx` at root `/`)
  - routeTree.gen.ts will be auto-regenerated by dev server once child routes are added (US-034+)
- Files changed: `src/routes/_client/route.tsx` (new), `src/actions/helpers.ts`, `src/routeTree.gen.ts`
- **How to verify:**
  - Run `bun run dev`
  - Add a `client_user_access` row for your user in the DB: `INSERT INTO client_user_access (client_id, user_id) VALUES ('<uuid>', '<userId>')`
  - Navigate to `/portal` — the simplified sidebar should appear (Home, Vencimientos, Deudas, Notificaciones, Solicitudes only; no admin/sueldos/jobs links)
  - Without a `client_user_access` row, navigate to `/portal` → should redirect to `/login`
- **Learnings for future iterations:**
  - `_client` pathless layout conflicts with `_authed` when both have no URL-prefix distinction — client portal children must use `/portal/*` paths
  - `bunx @tanstack/router-cli generate` fails with "Conflicting paths" when a new pathless layout has no children yet; manually update `routeTree.gen.ts` until child routes are added
  - `getClientPortalSession()` uses `getAuthSession()` (not `getSessionWithOrg()`) because portal users may not have an active org
  - The `routeTree.gen.ts` has `@ts-nocheck` but the `declare module` section IS type-checked — must add `FileRoutesByPath` entry to fix `createFileRoute` argument type errors
---

## 2026-04-24 - US-031
- Added `clientUserAccess` table to `drizzle/schema.ts`: id, clientId (FK client CASCADE), userId (FK user CASCADE), role (default 'client_viewer'), canUploadDocuments, canViewDebts, canViewIva, canViewPayroll (default false), canChatAi, createdAt. Unique on (clientId, userId)
- Added `clientRequest` table to `drizzle/schema.ts`: id, organizationId (FK org CASCADE), clientId (FK client CASCADE), profileId (FK profile SET NULL nullable), requestedByUserId (FK user SET NULL nullable), title, description, type, status (default 'open'), dueAt, completedAt, metadata (jsonb nullable), createdAt
- Created migration script `src/scripts/ensure-client-portal-tables.ts` using `CREATE TABLE IF NOT EXISTS` pattern for both tables
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-client-portal-tables.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-client-portal-tables.ts` (prints "Tablas client_user_access y client_request: OK.")
  - Verify in DB: `\d client_user_access` and `\d client_request` should show all columns
- **Learnings for future iterations:**
  - `clientUserAccess` uses a multi-column unique constraint defined in the table's second argument (array of constraints)
  - `clientRequest` does NOT have an `updatedAt` column (PRD spec didn't require it)
---

## 2026-04-24 - US-030
- Added `getBalanceConfig(clientId)` and `upsertBalanceConfig(clientId, data)` server functions to `src/actions/client.tsx`
- Added `clientBalanceConfig` import to the schema imports in client.tsx
- `getBalanceConfig` fetches the config for a client (validates client belongs to org); returns null if not configured
- `upsertBalanceConfig` uses `onConflictDoUpdate` on the unique `clientId` column to insert or update
- In `src/components/client-detail-page.tsx`, added "Cierre de ejercicio" card as Row 3 in the resumen tab
- Card has: month selector (1–12), day selector (1–31), optional presentation due days input, alert days before (comma-separated text input)
- State syncs from fetched config via `useEffect`; Save button calls `upsertBalanceConfigMutation`; shows current config date after save
- Files changed: `src/actions/client.tsx`, `src/components/client-detail-page.tsx`
- **How to verify:**
  - Run `bun run dev`, navigate to any client detail page (`/clients/$clientId`)
  - In the "Resumen" tab, scroll to the new "Cierre de ejercicio" card at the bottom
  - Set month/day (e.g. Diciembre / 31), optionally fill presentation days and alert days
  - Click "Guardar" → toast "Configuración guardada" appears; the card shows "Cierre: 31/12"
  - Reload the page — the saved values are pre-loaded from the DB
- **Learnings for future iterations:**
  - `clientBalanceConfig.clientId` is the unique conflict target for `onConflictDoUpdate` — single column, not array
  - `jsonb()` fields return `unknown` type; cast server fn return with `as ... & { alertDaysBefore: number[] } | null` to fix TS errors without `as any`
  - `useEffect` syncing state from a query result is the pattern used to populate form fields from server data in this codebase
---

## 2026-04-24 - US-029
- Created `src/routes/_authed/alerts/index.tsx` with full alert inbox page
- Added `AlertTriangle` NavItem in `src/components/app-sidebar.tsx` with open alert count badge
- Added `type` filter to `listAlerts` in `src/actions/alert.tsx`
- Page includes: filter bar (status, severity, type, client), alert list with SeverityBadge, type chip, title, description, client name, source entity type, time ago
- Each alert row has: Assign (inline member select dropdown), Resolve (mutates to resolved), View source (href to relevant page based on alert type)
- `getSourceHref` maps alert types to routes: overdue_debt→/clients/$id, critical_notification→/notifications, upcoming_due_date→/vencimientos, scraper_error→/jobs
- Sidebar queries open alerts with `staleTime: 60_000` to avoid excessive requests
- Files changed: `src/routes/_authed/alerts/index.tsx` (new), `src/actions/alert.tsx`, `src/components/app-sidebar.tsx`
- **How to verify:**
  - Run `bun run dev`, look at sidebar — new "Alertas" item appears below Vencimientos with red count badge if open alerts exist
  - Navigate to /alerts — see filter bar and alert list
  - Use filters (status, severity, type, client) to narrow down alerts
  - On an alert row: click "Asignar" → inline member select appears; pick a member → alert updates
  - Click "Resolver" → alert disappears from "Abiertas" filter view
  - Click "Ver" → navigates to the source page (e.g. /notifications for a critical_notification alert)
  - Switch status filter to "Resueltas" to see resolved alerts (grayed out)
- **Learnings for future iterations:**
  - `listAlerts` returns `as any` — cast to typed interface in the component for clean usage
  - Alert `type` field needed its own filter added to `listAlerts` inputValidator (was missing)
  - `relativeTime` and `ArcaCard` are both exported from `@/components/dashboard/shared` — usable across routes
  - For "View source" navigation with dynamic paths, use `<a href={...}>` rather than TanStack Router `<Link>` to avoid TS route-string constraints
  - Sidebar count badge uses `urgentCount` prop on NavItem — already supported by existing component
---

## 2026-04-24 - US-028
- Created `src/lib/risk-engine.ts` with `calculateRiskScore(profileId, period)` and `generateRiskSnapshots(orgId, period)`
- `calculateRiskScore` computes score 0-100 across 6 factors: overdue debt (30%), critical notifications (20%), upcoming due dates (15%), months without invoices (15%), IVA status (10%), scraper errors (10%)
- Risk levels: low < 25, medium 25-50, high 50-75, critical > 75
- `generateRiskSnapshots` iterates all org profiles and upserts into `profile_risk_snapshot` via `onConflictDoUpdate` on (profileId, period)
- Files changed: `src/lib/risk-engine.ts`
- **How to verify:**
  - Import `calculateRiskScore` in any server context and call with a valid `profileId` and `period` like `"2026-03"`
  - Returns `{ score, riskLevel, factors }` with component scores summing to total
  - Import `generateRiskSnapshots(orgId, "2026-03")` — inserts/updates rows in `profile_risk_snapshot`
  - Re-running `generateRiskSnapshots` for same period updates existing rows (no duplicates)
- **Learnings for future iterations:**
  - `onConflictDoUpdate` with `target: [col1, col2]` works in Drizzle ORM for multi-column unique constraints
  - `invoice.profile` is the FK column name on the invoice table (not `invoice.profileId`) — matches the `.client` naming pattern of other tables
  - `ivaScrape.periodoFiscal` stores periods in the format from the scraper — use whatever format the caller passes consistently
  - `sql<number>\`COUNT(*)\`` pattern used for aggregations to match codebase convention (not `count()` from drizzle-orm)
---

## 2026-04-24 - US-027
- Created `src/lib/alert-generator.ts` with `generateAlerts(orgId)` function
- Scans 4 sources: overdue open debts (dueDate < now), critical unresolved notifications, upcoming due dates within 7 days (not completed), clients with hasErrors=true
- Deduplicates using a Set of fingerprints: `${type}:${sourceEntityType}:${sourceEntityId}` — loads existing open alerts first, then skips any already covered
- Alert severities: overdue_debt=high, critical_notification=critical, upcoming_due_date=medium, scraper_error=high
- Files changed: `src/lib/alert-generator.ts`
- **How to verify:**
  - Import and call `generateAlerts(orgId)` from any server context (e.g., a test server function)
  - Returns `{ created: N }` where N is the number of new alerts inserted
  - Re-running with same org produces `{ created: 0 }` (deduplication working)
  - Check `alert` table: new rows with correct type, severity, sourceEntityType, sourceEntityId
- **Learnings for future iterations:**
  - `debt.client`, `notification.client`, `dueDate.client` are the FK column names (not `.clientId`) — use `inArray(debt.client, clientIds)` etc.
  - Deduplication loads existing open alerts once then uses in-memory Set — avoids N+1 queries
  - `alert.$inferInsert` is the correct type for insert values with Drizzle
---

## 2026-04-24 - US-026
- Created `src/actions/alert.tsx` with all required CRUD functions: `listAlerts`, `createAlert`, `acknowledgeAlert`, `assignAlert`, `resolveAlert`, `bulkResolveAlerts`
- All functions scope by `orgId` from `getSessionWithOrg()`; write mutations use `assertCanWrite(role)`
- Fixed jsonb `metadata` type mismatch by casting returns `as any` (same pattern as `agent.tsx`)
- Files changed: `src/actions/alert.tsx`
- **How to verify:**
  - Run `bun run dev` and import `listAlerts` from `@/actions/alert` in any component
  - Call `createAlert({ type: 'overdue_debt', severity: 'critical', title: 'Test' })` via server function
  - Call `listAlerts({ status: 'open' })` to confirm it returns the new alert
  - Call `resolveAlert({ id })` and verify `status` becomes `'resolved'`
- **Learnings for future iterations:**
  - `jsonb()` fields in Drizzle return `unknown` type; cast return values `as any` in server function handlers to fix TypeScript errors (see `agent.tsx` line 85 for pattern)
  - The `alert` table uses `organizationId` (not `orgId`) as the column name — filter with `eq(alertTable.organizationId, orgId)`
---

## 2026-04-24 - US-025
- Added `clientBalanceConfig` table to `drizzle/schema.ts`: id, clientId (FK unique CASCADE), fiscalYearEndMonth, fiscalYearEndDay, presentationDueDays (nullable), alertDaysBefore (jsonb default [60,30,15,7]), createdAt, updatedAt
- Used `jsonb` for `alertDaysBefore` since native integer arrays aren't used in this schema (simpler)
- Created migration script `src/scripts/ensure-client-balance-config-table.ts`
- Migration ran successfully
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-client-balance-config-table.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-client-balance-config-table.ts` (prints "Tabla client_balance_config: OK.")
- **Learnings for future iterations:**
  - Use `jsonb` instead of `integer[]` for array-like data — no `array()` import needed, simpler
---

## 2026-04-24 - US-024
- Added `profileRiskSnapshot` table to `drizzle/schema.ts`: id, profileId (FK profile CASCADE), period, score (numeric 5,2), riskLevel, factors (jsonb), createdAt
- Unique constraint on (profileId, period) for upsert pattern
- Created migration script `src/scripts/ensure-profile-risk-snapshot-table.ts`
- Migration ran successfully
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-profile-risk-snapshot-table.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-profile-risk-snapshot-table.ts` (prints "Tabla profile_risk_snapshot: OK.")
---

## 2026-04-24 - US-023
- Added `alert` table to `drizzle/schema.ts` with all required fields: id, organizationId (FK org CASCADE), clientId (FK client CASCADE nullable), profileId (FK profile SET NULL nullable), type, severity, title, description, sourceEntityType, sourceEntityId, status (default 'open'), assignedToUserId (FK user SET NULL nullable), dueAt, resolvedAt, resolvedByUserId (FK user SET NULL nullable), metadata (jsonb nullable), createdAt, updatedAt
- Added `idx_alert_org_status` index on (organizationId, status)
- Created migration script `src/scripts/ensure-alert-table.ts` using `CREATE TABLE IF NOT EXISTS` pattern
- Migration ran successfully (timeout at 60s as expected)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-alert-table.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-alert-table.ts` (prints "Tabla alert: OK.")
  - Verify in DB: `\d alert` should show all columns and `\d idx_alert_org_status` for the index
- **Learnings for future iterations:**
  - `alert` is a reserved word in some SQL dialects but PostgreSQL handles quoted table names fine
  - `clientId` uses CASCADE delete (alert disappears when client is deleted), `profileId` uses SET NULL
  - `assignedToUserId` and `resolvedByUserId` both reference user table with SET NULL on delete
---

## 2026-04-24 - US-022
- Added `toolCallDescriptions` mapping tool names to human-readable descriptions
- Extended `mergedMessages` type to include `toolCalls?: { toolName, args }[]` extracted from `tool-invocation` parts
- Added `expandedToolCalls` state (Set<string>) and `toggleToolCalls` function to track collapsed/expanded per message
- Added "Fuentes consultadas" collapsible section above ReactMarkdown in assistant messages:
  - Only shown when `msg.toolCalls` is non-empty
  - Toggle button with ChevronRight/ChevronDown icon and count
  - Expanded: shows gray box with each tool as `{ monospace code tag } description` rows
- Added `ChevronDown`, `ChevronRight` to lucide imports
- Files changed: `src/routes/_authed/chat/$id.tsx`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`, ask a question that triggers tool use (e.g. "¿Cuál es el estado de [cliente]?")
  - After the response appears: look for "Fuentes consultadas (N)" link above the message text
  - Click it: gray box expands showing the tool names used (e.g. `get_client_summary` → "Resumen del cliente")
  - Click again to collapse
- **Learnings for future iterations:**
  - Tool invocation parts have `type: 'tool-invocation'` with nested `toolInvocation.toolName` and `toolInvocation.args`
  - `(p as any).toolInvocation?.toolName` is needed because AIMessage parts types don't include toolInvocation in their union
  - Messages with only tool-invocation parts and no text parts return empty `text` → the current `if (!text) return acc` guard skips them; tool calls are accumulated correctly when the next text part arrives in the merged loop
---

## 2026-04-24 - US-021
- Added `payroll_preview_receipt` tool in `src/routes/api/agent.ts`
- Added imports: `payrollEscala`, `payrollConvenioCategoria`, `payrollConcepto` from schema; `or` from drizzle-orm; `differenceInYears` from date-fns; `evaluatePayrollFormulaStrict` from `@/lib/payroll-formula`
- Tool accepts: `clientName`, `employeeName`, `periodo` (YYYY-MM)
- Flow: find client → find active employee (fechaBaja IS NULL) → get basico from payrollEscala (vigenciaDesde <= period AND vigenciaHasta IS NULL OR >= period) → get conceptos → run formula evaluation loop in-memory → return totals
- Returns structured JSON: `{ empleado, cuil, categoria, cliente, periodo, basico, haberes, noRemunerativo, descuentos, retenciones, neto, conceptos: [...], nota }`
- Does NOT persist anything to DB — uses `dbReadonly` throughout
- Updated agent system prompt to mention `payroll_preview_receipt`
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`
  - Ask "¿Cuánto cobraría [empleado] de [cliente] en [mes] YYYY?" → agent calls `payroll_preview_receipt`
  - Response shows basico, haberes, descuentos, retenciones, neto and concept breakdown
  - No new rows created in liquidacion_import_recibo table
- **Learnings for future iterations:**
  - `payrollEscala` vigency check: `lte(vigenciaDesde, periodoDate)` AND `or(isNull(vigenciaHasta), gte(vigenciaHasta, periodoDate))` — need to import `or` from drizzle-orm
  - `differenceInYears` is the right function from date-fns for seniority calculation (same as sueldos.ts)
  - `evaluatePayrollFormulaStrict` never throws — always returns `{ ok, value, error }`
  - Formula context must be updated incrementally (totalRemunerativo, neto, etc.) so each concept can reference accumulated values from previous concepts
  - `payrollConcepto.clientId` uses `clientId` naming (consistent with `job.clientId`) unlike most other tables that use `.client`
---

## 2026-04-24 - US-020
- Added `get_profile_status` tool in `src/routes/api/agent.ts`
- Added `liquidacionImportEmpleado` to schema imports
- Accepts: `clientName` (required), `profileName` (optional)
- Per profile: runs 4 parallel queries: last IVA scrape, open notification count, active employee count (fechaBaja IS NULL), last finished jobs per type
- Returns: `{ cliente, perfiles: [{ perfil, cuit, estado, administradoPorEstudio, tipoPerfil, liquidaSueldos, ultimoScrapeo, ultimoIvaScrape, notificacionesAbiertas, empleadosActivos, ultimosScrapeos }] }`
- Updated agent system prompt to mention `get_profile_status`
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`
  - Ask "¿Cuál es el estado del perfil de [cliente]?" → agent calls `get_profile_status`
  - Ask "¿El perfil [perfil] de [cliente] está actualizado?" → agent calls with profileName filter
  - Response shows IVA scrape, notificaciones abiertas, empleados activos, tipo de perfil
- **Learnings for future iterations:**
  - `liquidacionImportEmpleado.profileId` (not `.profile`) — this table uses standard naming unlike other tables that use `.client` for FK
  - Active employees: `isNull(liquidacionImportEmpleado.fechaBaja)` — no `activo` boolean on this table, only fechaBaja
  - `notification.profile` is the FK column (not profileId) — consistent with other tables using `.client` pattern
  - `job.clientId` is the FK column on job table (inconsistent with other tables using `.client`)
---

## 2026-04-24 - US-019
- Added `get_due_dates` tool in `src/routes/api/agent.ts`
- Accepts: `clientName` (optional), `days_ahead` (default 30, max 365), `include_completed` (default false)
- Resolves client IDs from org, filters `due_date` joined with `client`, applies date range and optional completedAt filter
- Returns structured JSON: `{ cliente, total, dueDates: [{ id, impuesto, concepto, periodo, vencimiento, completadoEl, diasHastaVencimiento, cliente }] }`
- `diasHastaVencimiento` computed as ceil((dueDate - now) / ms_per_day)
- Sorted by dueDate ascending
- Updated agent system prompt to mention `get_due_dates`
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`
  - Ask "¿Cuáles son los próximos vencimientos de [cliente]?" → agent calls `get_due_dates` with clientName
  - Ask "¿Hay vencimientos en los próximos 7 días?" → agent calls `get_due_dates` with days_ahead=7
  - Ask "Mostrame todos los vencimientos incluyendo los completados" → agent calls with include_completed=true
- **Learnings for future iterations:**
  - `dueDate.dueDate` is the column field and also the table import — use `dueDateVal` alias in select to avoid naming collision
  - `gte(dueDate.dueDate, now)` filters to upcoming vencimientos — no need for `isNull(completedAt)` when include_completed=false (handled separately)
  - Same `inArray` pattern as `get_debts`: use `any[]` for conditions array to avoid type complexity
---

## 2026-04-24 - US-018
- Added `get_debts` tool in `src/routes/api/agent.ts`
- Accepts: `clientName` (optional, partial ILIKE match), `status` (optional enum: open/in_plan/paid/disputed), `limit` (default 20, max 200)
- Resolves client IDs from org, filters `debt` joined with `client`, applies optional status filter
- Returns structured JSON: `{ cliente, total, debts: [{ id, impuesto, concepto, periodo, vencimiento, capital, interesesResarcitorios, interesesPunitorios, totalDeuda, estado, intimada, cliente }] }`
- `totalDeuda` computed as `balance + compensatoryInterest + punitiveInterest`
- Updated agent system prompt to mention `get_debts`
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`
  - Ask "¿Qué deudas tiene [cliente]?" → agent should call `get_debts` with clientName
  - Ask "¿Hay deudas abiertas en la org?" → agent calls `get_debts` without clientName
  - Ask "Mostrá las deudas en plan de pagos" → agent calls `get_debts` with status='in_plan'
- **Learnings for future iterations:**
  - `debt.client` is the FK column name (not `debt.clientId`) — matches the pattern used by other tables (notification.client, dueDate.client)
  - `inArray(debt.client, clientIds)` requires `as any` cast when mixed with `eq()` in a typed conditions array — or just use an untyped array like the `get_open_notifications` pattern
  - `balance`, `compensatoryInterest`, `punitiveInterest` come as strings from Drizzle `numeric` columns — use `parseFloat()` to compute totals
---

## 2026-04-24 - US-017
- Added `get_open_notifications` tool in `src/routes/api/agent.ts`
- Accepts: `clientName` (optional, partial ILIKE match), `severity` (optional enum), `limit` (default 10, max 100)
- Resolves client IDs from org, filters `notification` joined with `client`, applies `profile IS NOT NULL` and `resolvedAt IS NULL` conditions
- Returns structured JSON: `{ cliente, total, notifications: [{ id, mensaje, severidad, categoria, resumen, fechaPublicacion, leida, cliente }] }`
- Added `inArray` to drizzle-orm imports
- Updated agent system prompt to mention `get_open_notifications`
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`
  - Ask "¿Cuáles son las notificaciones críticas de [cliente]?" → agent should call `get_open_notifications` with severity='critical'
  - Ask "¿Hay notificaciones sin resolver?" → agent should return open notifications across all clients
  - Ask "Mostrame las últimas 5 notificaciones de [cliente]" → tool called with limit=5
- **Learnings for future iterations:**
  - `inArray(notification.client, clientIds)` is used instead of a JOIN with `orgId` filter — because client IDs are already resolved to org scope
  - `isNotNull(notification.profile)` is required per schema convention (NULL profile = notification from another org's scrape)
  - `isNull(notification.resolvedAt)` filters to only open/unresolved notifications
  - For optional clientName filtering: load all org clients first, then filter in-memory with `.toLowerCase().includes()` for case-insensitive partial match
---

## 2026-04-24 - US-013
- Added `sourceConfidence` (text default 'unknown') and `importedManually` (boolean default false) columns to `ivaScrape` table in `drizzle/schema.ts`
- Created migration script `src/scripts/ensure-iva-scrape-source-columns.ts` using `ADD COLUMN IF NOT EXISTS` pattern
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-iva-scrape-source-columns.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-iva-scrape-source-columns.ts` (should print "Columnas de origen de iva_scrape: OK.")
  - Verify in DB: `\d iva_scrape` should show `source_confidence` (text, default 'unknown') and `imported_manually` (boolean, default false) columns
- **Learnings for future iterations:**
  - Migration timed out at 60s but printed success — DB operation completed, timeout just terminates the process. Expected behavior per prior learnings.
  - No new imports needed in schema.ts for `text` and `boolean` — already imported
---

## 2026-04-24 - US-012
- Added 5 storage metadata columns to `document` table in `drizzle/schema.ts`: storageProvider (text default 'external'), storageKey (text nullable), mimeType (text nullable), sizeBytes (integer nullable), checksum (text nullable)
- Created migration script `src/scripts/ensure-document-storage-columns.ts` using `ADD COLUMN IF NOT EXISTS` pattern
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-document-storage-columns.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-document-storage-columns.ts` (should print "Columnas de almacenamiento de document: OK.")
  - Verify in DB: `\d document` should show 5 new columns: storage_provider, storage_key, mime_type, size_bytes, checksum
- **Learnings for future iterations:**
  - `integer` type is already imported in drizzle/schema.ts — no new imports needed for integer columns
  - Pre-existing typecheck errors in drizzle/relations.ts, drizzle/enums.ts, credential.tsx are unrelated to our changes
---

## 2026-04-24 - US-011b
- Created `src/components/dashboard/exceptions-bar.tsx` with `ExceptionsBar` component
- Shows up to 4 colored alert cards (overdueDebt=red, criticalNotifications=red, upcomingDueDates=warn, clientErrors=warn)
- Cards only render when count > 0; if all counts are 0, component returns null
- Each card is a `<Link>` navigating to: debts→/clients, notifications→/notifications, vencimientos→/vencimientos, errors→/clients
- Wired into `src/routes/_authed/index.tsx` between `<DashboardGreeting>` and `<KpiCardsRow>`
- Files changed: `src/components/dashboard/exceptions-bar.tsx`, `src/routes/_authed/index.tsx`
- **How to verify:**
  - Run `bun run dev`, go to the dashboard (/)
  - If there are overdue debts, critical notifications, upcoming due dates, or clients with errors: colored alert cards appear below the greeting
  - Click a card → navigates to the relevant page
  - Cards only appear for non-zero counts
- **Learnings for future iterations:**
  - `getExceptionsSummary` returns immediately; wrap in `useQuery` with `staleTime: 60_000` to avoid excessive re-fetching
  - Arca color tokens used: `--arca-accent-neg` / `--arca-accent-neg-bg` for red, `--arca-accent-warn` / `--arca-accent-warn-bg` for orange
  - TanStack Router `<Link to="...">` requires string literal paths that match registered routes
---

## 2026-04-24 - US-011a
- Added `getExceptionsSummary` server function in `src/actions/dashboard.tsx`
- Runs 4 parallel COUNT queries: overdue open debts, critical unresolved notifications, upcoming due dates within 3 days not completed, clients with hasErrors=true
- Returns `{ overdueDebtCount, criticalNotificationCount, upcomingDueDateCount, clientErrorCount }`
- Added `isNull` import from drizzle-orm
- Files changed: `src/actions/dashboard.tsx`
- **How to verify:**
  - Import and call `getExceptionsSummary()` from any dashboard component
  - Returns 4 counts scoped to the calling user's org
  - `overdueDebtCount`: debts with status='open' AND dueDate <= today
  - `criticalNotificationCount`: notifications with severity='critical' AND resolvedAt IS NULL
  - `upcomingDueDateCount`: due dates within next 3 days AND completedAt IS NULL
  - `clientErrorCount`: clients with hasErrors=true
- **Learnings for future iterations:**
  - All 4 queries run in parallel via `Promise.all` for efficiency
  - Early return pattern when `userClientIds.length === 0` avoids unnecessary DB hits
  - `clientErrorCount` queries directly on `client` table with `eq(client.organizationId, orgId)` — no need to filter via `inArray(clientIds)` since we already have orgId
---

## 2026-04-24 - US-010b
- Updated `getCalendarDueDates` in `src/actions/dashboard.tsx` to return `completedAt` for due dates
- Updated `src/components/vencimientos-calendar.tsx`:
  - Added `useMutation` + `useQueryClient` imports; imported `markDueDateCompleted` from `@/actions/client`
  - Added `completedAt` field to `CalendarEvent` interface
  - Populated `completedAt` when building events map from due dates
  - In detail panel: replaced icon div with toggle button (Circle/CheckCircle2) for `due` events; title and subtitle show `line-through` when completed; completed rows use green tinted background with opacity-70
  - In calendar grid: each event pill shows green background with strikethrough when `completedAt` is set
  - `completeMutation` invalidates `calendarQueryKey` on success for instant UI refresh
- Files changed: `src/actions/dashboard.tsx`, `src/components/vencimientos-calendar.tsx`
- **How to verify:**
  - Run `bun run dev`, go to the Vencimientos page (calendar view)
  - Click on a day with due dates in the detail panel on the left
  - Each due date shows a circle icon button — click it to mark as completed
  - Completed item: title/subtitle show strikethrough, background turns green, icon becomes a filled CheckCircle2
  - Calendar grid pill for that event turns green with strikethrough
  - Click the CheckCircle2 again to unmark (toggle back to pending)
- **Learnings for future iterations:**
  - `getCalendarDueDates` returns both dueDates and debts — only dueDates have completion; debts still show AlertTriangle icon unchanged
  - Query invalidation uses the exact same key array (`calendarQueryKey`) — ensure `fetchFrom`/`fetchTo` are stable references (they are, derived from `gridDays` memo)
  - `arca-accent-pos` CSS vars used for green: `--arca-accent-pos-bg`, `--arca-accent-pos-fg` — consistent with other status colors in the codebase
---

## 2026-04-24 - US-010a
- Added `completedAt` (timestamp nullable) and `completedByUserId` (text FK user set null) columns to `dueDate` table in `drizzle/schema.ts`
- Created migration script `src/scripts/ensure-due-date-completion-columns.ts` using `ADD COLUMN IF NOT EXISTS` pattern
- Added `markDueDateCompleted(id, completed)` server function in `src/actions/client.tsx` (POST, canWrite required): validates org ownership via JOIN with client table, sets completedAt/completedByUserId when completing, clears them when un-completing
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-due-date-completion-columns.ts`, `src/actions/client.tsx`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-due-date-completion-columns.ts` (should print "Columnas de completitud de due_date: OK.")
  - Verify in DB: `\d due_date` should show `completed_at` and `completed_by_user_id` columns
  - Call `markDueDateCompleted({ id: '<uuid>', completed: true })` — row should have completedAt set and completedByUserId set to calling user's ID
  - Call `markDueDateCompleted({ id: '<uuid>', completed: false })` — row should have both fields cleared to null
- **Learnings for future iterations:**
  - `markDueDateCompleted` accepts `completed: boolean` to toggle — this enables an unmark action (US-010b will need this for the toggle UI)
  - `getSessionWithOrg()` returns `userId` directly — no extra lookup needed for completedByUserId
  - Due date ownership validated by JOINing with client table and checking `client.organizationId = orgId`
---

## 2026-04-24 - US-009
- Added `updateDebtStatus` server function in `src/actions/client.tsx` (POST, canWrite required): validates org ownership via JOIN with client table, updates status and isIntimated on the debt row
- Updated debt table in `src/components/client-detail-page.tsx`:
  - Added "Gestión" column header alongside existing "Estado"
  - Per-row: `<select>` dropdown with options Abierta/En plan/Pagada/Disputada (calls `updateDebtStatusMutation`)
  - Per-row: "Intimada" toggle button (orange when active, neutral when not)
  - Row background color-coded: open+overdue=red tint, intimated=orange tint, paid=green tint, in_plan=gray tint
  - Status badge updated: shows Pagada/En plan/Disputada/Vencida/Abierta with matching colors
  - Orange "Intimada" badge shown inline next to status badge when isIntimated=true
- Added `updateDebtStatusMutation` useMutation hook in component
- Files changed: `src/actions/client.tsx`, `src/components/client-detail-page.tsx`
- **How to verify:**
  - Run `bun run dev`, go to any client page → Deudas tab
  - Each debt row now has a status dropdown (Abierta/En plan/Pagada/Disputada) and "Intimada" toggle button
  - Change a debt to "Pagada" → row turns green tinted; to "En plan" → row turns gray; to an overdue open debt → row turns red tinted
  - Click "Intimada" button → row turns orange tinted, orange "Intimada" badge appears next to status badge
  - Mutations invalidate `['clientDebts', clientId]` query key, so UI refreshes automatically
- **Learnings for future iterations:**
  - Debt validation: JOIN `debt` with `client` using `innerJoin` to verify `client.organizationId = orgId` before mutating
  - Row color-coding uses inline `style={{ background: ... }}` with rgba values since Tailwind dynamic classes aren't supported
  - `debt.isIntimated` and `debt.status` come directly from the schema (added in US-008) — no casting needed on schema side
---

## 2026-04-24 - US-008
- Added 4 columns to `debt` table in `drizzle/schema.ts`: status (text default 'open'), detected_at (timestamp default now()), source_period (text nullable), is_intimated (boolean default false)
- Created migration script `src/scripts/ensure-debt-enrichment-columns.ts` using `ADD COLUMN IF NOT EXISTS` pattern
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-debt-enrichment-columns.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-debt-enrichment-columns.ts` (should print "Columnas de enriquecimiento de debt: OK.")
  - Verify in DB: `\d debt` should show the 4 new columns (status, detected_at, source_period, is_intimated)
- **Learnings for future iterations:**
  - Migration timed out at 60s but still printed success — the DB operation completed, the timeout just terminates the process after. This is expected behavior.
  - Pre-existing typecheck errors in drizzle/relations.ts, drizzle/enums.ts, src/actions/credential.tsx — not related to our changes
---

## 2026-04-24 - US-006
- Added `SeverityBadge` component inline in `notifications-view.tsx` (critical=red, medium=orange, low=blue, informational=gray, unclassified=hidden)
- Added `categoryFilter` state and `SearchableSelect` dropdown for category filtering (Todos, Requerimiento, Intimación, Deuda, Vencimiento, Comunicación, Inspección, Otro)
- Updated `getNotifications` in `src/actions/notification.tsx` to accept `category` filter param and return `severity`, `category`, `aiSummary` fields in select
- Added severity-based sort: critical(0) > medium(1) > low(2) > informational(3) > unclassified(4), secondary sort by publicationDate desc
- Shows `aiSummary` as italic subtitle below the profile/client name when available
- Files changed: `src/actions/notification.tsx`, `src/components/notifications-view.tsx`
- **How to verify:**
  - Run `bun run dev`, navigate to /notifications
  - Notifications with AI classification should show colored severity badge (red=critical, orange=medium, blue=low) next to the client name
  - Category dropdown appears below client/profile filter — selecting a category filters the list
  - Notifications are sorted by severity (critical first) within date groups
  - If any notification has ai_summary, it shows as italic text below the badge row
- **Learnings for future iterations:**
  - `getNotifications` query key includes `categoryFilter` — remember to invalidate with matching key structure when mutating notifications
  - Severity sort is client-side (after fetch) to avoid complicating the SQL ORDER BY
  - `(notification as any).severity` cast needed because `NotificationData` interface is unused but the actual runtime data is typed from server fn return
---

## 2026-04-24 - US-005
- Added `classifyWithGemini(message)` internal helper in `src/actions/notification.tsx` that calls Gemini Flash with a structured JSON response schema to classify severity, category, and generate ai_summary
- Added `classifyNotification` server function (POST, canWrite required): validates org ownership, calls Gemini, updates notification row (severity, category, aiSummary, aiClassifiedAt), logs to data_source_event with source='ai', action='classified'
- Added `classifyUnclassifiedNotifications` server function (POST, canWrite required): fetches all notifications with severity='unclassified' AND aiClassifiedAt IS NULL for the org, classifies each one, returns { classified, errors } counts
- Added imports: `GoogleGenAI` from `@google/genai`, `dataSourceEvent` from schema, `isNull` from drizzle-orm
- Files changed: `src/actions/notification.tsx`
- **How to verify:**
  - Run `bun run dev`, ensure GEMINI_API_KEY is set in .env
  - Call `classifyNotification({ id: '<notification-uuid>' })` from a component or via server function — notification row should update with severity/category/ai_summary
  - Call `classifyUnclassifiedNotifications()` — all unclassified notifications for the org get classified in batch
  - Verify data_source_event table gets a row with source='ai', action='classified', entity_type='notification'
- **Learnings for future iterations:**
  - Gemini pattern: `new GoogleGenAI({ apiKey })`, then `ai.models.generateContent({ model: 'gemini-2.0-flash', contents: [...], config: { responseMimeType: 'application/json', responseSchema: {...} } })`
  - dataSourceEvent insert: `clientId: notif.clientId ?? undefined` (not null — drizzle expects undefined for optional FK fields)
  - classifyUnclassifiedNotifications uses `isNull(notification.aiClassifiedAt)` AND `eq(notification.severity, 'unclassified')` to avoid re-classifying already-processed notifications
---

## 2026-04-24 - US-007
- Added `listOrgMembersForAssignment` server function in `src/actions/notification.tsx` — accessible to all org members (not just owners, unlike `getOrgMembers` in admin.tsx)
- Added `assignNotification(id, userId)` server function (POST, canWrite): sets `assignedToUserId`, accepts null to unassign
- Added `resolveNotification(id)` server function (POST, canWrite): sets `resolvedAt` and `resolvedByUserId` from session
- Added `unresolveNotification(id)` server function (POST, canWrite): clears `resolvedAt` and `resolvedByUserId`
- Updated `getNotifications` to: accept `onlyUnresolved` filter (adds `isNull(resolvedAt)` condition), return `assignedToUserId`, `resolvedAt`, `resolvedByUserId` fields
- In `notifications-view.tsx`:
  - Added "Solo sin resolver" toggle button filter in left panel (highlights primary when active)
  - Resolved notifications show with `opacity-50` muted style in the list
  - Right panel detail section: "Asignar a" dropdown (SearchableSelect with org members, includes "Sin asignar") + "Resolver"/"Reabrir" button
  - Added `assignMutation`, `resolveMutation`, `unresolveMutation` mutations
  - Added `orgMembers` query using `listOrgMembersForAssignment`
- Files changed: `src/actions/notification.tsx`, `src/components/notifications-view.tsx`
- **How to verify:**
  - Run `bun run dev`, navigate to /notifications
  - Select a notification — right panel shows "Asignar a" dropdown and "Resolver" button
  - Assign to a team member: dropdown updates, member saved
  - Click "Resolver": button changes to "Reabrir", notification becomes muted in list
  - Toggle "Solo sin resolver" in left panel: resolved notifications disappear from list
  - Click "Reabrir": notification returns to open state
- **Learnings for future iterations:**
  - `getOrgMembers` in `src/actions/admin.tsx` requires `requireOwner()` — cannot be used for general member lookups; created `listOrgMembersForAssignment` in notification.tsx instead using `getSessionWithOrg()`
  - `resolvedByUserId` is set from `userId` returned by `getSessionWithOrg()` — no extra session lookup needed
  - `isNull()` from drizzle-orm is used for `WHERE col IS NULL` conditions
---

## 2026-04-24 - US-014
- Added 4 columns to `agentMessage` table in `drizzle/schema.ts`: metadata (jsonb nullable), toolCalls (jsonb nullable), citations (jsonb nullable), confidence (text nullable)
- Created migration script `src/scripts/ensure-agent-message-columns.ts` using `ADD COLUMN IF NOT EXISTS` pattern
- Updated `onFinish` callback in `src/routes/api/agent.ts`: extracts tool-invocation parts from assistant messages into `toolCalls` array, builds `metadata` object with stepCount/toolCallCount, persists both on the assistant `agentMessage` row
- Updated `getConversationMessages` in `src/actions/agent.tsx` to explicitly select all columns including the new ones; used `return rows as any` to satisfy TanStack server function type inference (jsonb → `unknown` vs `{}` mismatch)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-agent-message-columns.ts`, `src/routes/api/agent.ts`, `src/actions/agent.tsx`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-agent-message-columns.ts` (prints "Columnas de agent_message: OK.")
  - Verify in DB: `\d agent_message` should show `metadata`, `tool_calls`, `citations`, `confidence` columns
  - Run `bun run dev`, open the chat at `/chat/<id>`, ask a question that triggers tool use (e.g. "¿Cuánto facturó [cliente] este mes?")
  - Query: `SELECT tool_calls, metadata FROM agent_message WHERE role = 'assistant' ORDER BY created_at DESC LIMIT 1` — should show tool_calls array and metadata with stepCount
- **Learnings for future iterations:**
  - `jsonb` columns in Drizzle return `unknown` type; TanStack server fn inference expects `{}` — fix with `return rows as any` rather than fighting the type system
  - Tool-invocation parts in AI SDK UIMessages have `type: 'tool-invocation'` with nested `toolInvocation.toolName` and `toolInvocation.args`
  - `as any` cast in agent.tsx is the same pattern that credential.tsx effectively needs but hasn't been fixed — acceptable workaround
---

## 2026-04-24 - US-015
- Added `agentRun` table to `drizzle/schema.ts` with all required fields: id, conversationId (FK agentConversation), userId (FK user), organizationId (FK organization), clientId (FK client nullable), profileId (FK profile nullable), status (text default 'running'), intent, input, output, toolTrace (jsonb), error, startedAt (timestamp default now()), finishedAt (timestamp nullable)
- Created migration script `src/scripts/ensure-agent-run-table.ts` using `CREATE TABLE IF NOT EXISTS` pattern
- Updated `src/routes/api/agent.ts`: inserted `agentRun` row before streaming (status='running'), updated it on `onFinish` with status='finished', output, toolTrace, finishedAt; marks status='failed' with error on exception
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-agent-run-table.ts`, `src/routes/api/agent.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-agent-run-table.ts` (prints "Tabla agent_run: OK.")
  - Verify in DB: `\d agent_run` should show all columns including status, input, output, tool_trace, started_at, finished_at
  - Run `bun run dev`, open `/chat/<id>`, send a message — query `SELECT status, input, output, tool_trace FROM agent_run ORDER BY started_at DESC LIMIT 1` should show a finished row with tool_trace data
- **Learnings for future iterations:**
  - `agentRunRow` is returned from `.returning({ id: agentRun.id })` — must destructure as `const [agentRunRow]` from the insert
  - The `agentRunRow` variable is closed over in `onFinish` callback — declaring it before the streaming call works correctly
  - `clientId` and `profileId` are nullable on `agentRun` — useful for future stories that detect client context from user input
---

## 2026-04-24 - US-016
- Added `get_client_summary` tool in `src/routes/api/agent.ts`
- Tool accepts `clientName` (string, partial ILIKE match), scoped to `orgId`
- Runs 5 parallel Drizzle queries via `Promise.all`: profile count, open notification count, overdue debt count + total balance, upcoming due dates (next 30 days, not completed), last finished job per type
- Returns structured JSON: cliente (datos generales), perfiles, notificaciones, deudas, proximosVencimientos, ultimosScrapeos
- Added imports: `notification`, `debt`, `dueDate`, `job` from schema; `isNull`, `isNotNull`, `lt`, `desc` from drizzle-orm
- Updated agent system prompt to mention the new tool
- Files changed: `src/routes/api/agent.ts`
- **How to verify:**
  - Run `bun run dev`, open `/chat/<id>`, ask "¿Cuál es el estado de [nombre_cliente]?"
  - Agent should call `get_client_summary` and return a structured summary with profile count, open notifications, overdue debts, upcoming due dates, and last scrape timestamps
  - Verify scope: tool only returns data for clients in the calling user's organization
- **Learnings for future iterations:**
  - All table FK columns are named `client` (not `clientId`) in schema except for `job` which uses `clientId` — double-check per table
  - `sql<number>` template is the codebase pattern for COUNT aggregations; `sql<string>` for SUM (PostgreSQL returns numerics as strings)
  - `isNotNull()` from drizzle-orm works for `WHERE col IS NOT NULL` — same pattern as `isNull()`
  - Agent tool names with underscores (`get_client_summary`) work fine; camelCase also works
  - Pre-existing TS errors in agent.ts: unused imports (INVOICE_TYPES_A/B, CREDIT_NOTE_TYPES) and unused destructure vars (netoIn5, netoIn25) — not introduced by our changes
---

## 2026-04-24 - US-048
- Added 3 accounting tables to `drizzle/schema.ts`:
  - `accountingAccount`: per-client chart of accounts with code, name, type, parent_id (self-referential FK via `foreignKey()`), active flag. Unique on (client_id, code)
  - `journalEntry`: double-entry journal entries with entry_date, description, source_type/source_id, status (default 'draft'), created_by_user_id
  - `journalEntryLine`: individual debit/credit lines per entry; references accountingAccount with ON DELETE RESTRICT
- Created `src/scripts/ensure-accounting-tables.ts` migration script (idempotent, uses CREATE TABLE IF NOT EXISTS for all 3 tables)
- Ran migration script — all 3 tables OK (exit 124 expected)
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-accounting-tables.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d accounting_account'` to confirm table with all columns including self-referential parent_id
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\dt accounting_%'` to see all accounting tables
  - Or: `timeout 60 bun run src/scripts/ensure-accounting-tables.ts` — prints OK for each table
- **Learnings for future iterations:**
  - Self-referential FK in Drizzle requires the `foreignKey()` helper in the table constraints array — `.references()` on the column itself doesn't work for the same table
  - `ON DELETE RESTRICT` for `journalEntryLine.accountId` prevents deleting accounts that have journal lines (correct accounting behavior)
  - `numeric` default values must be quoted strings in Drizzle: `.default("0")` not `.default(0)`
---

## 2026-04-24 - US-049
- Created `src/actions/accounting.tsx` with all 8 accounting server functions:
  - `listAccounts(clientId)`: GET, returns chart of accounts ordered by code
  - `createAccount(clientId, code, name, type, parentId?)`: POST, requires canWrite, validates client and optional parent belong to org, inserts account
  - `updateAccount(id, name?, active?)`: POST, requires canWrite, validates account belongs to org, updates name and/or active flag
  - `createJournalEntry(clientId, entryDate, description, lines[])`: POST, requires canWrite, validates double-entry balance (sum debits = sum credits, tolerance 0.001), validates all accounts belong to client, inserts entry + lines atomically
  - `listJournalEntries(clientId, from?, to?)`: GET, returns entries ordered by date desc
  - `getJournalEntry(id)`: GET, returns entry + lines joined with account code/name/type
  - `getLedger(clientId, accountId, from?, to?)`: GET, returns all lines for a specific account joined with entry metadata + totals
  - `getTrialBalance(clientId, from, to)`: GET, returns all active accounts with aggregated debit/credit totals + grand totals
- All functions call `getSessionWithOrg()` and scope via org ownership
- Private helpers: `ensureClientBelongsToOrg`, `ensureAccountBelongsToOrg`, `ensureJournalEntryBelongsToOrg`
- Files changed: `src/actions/accounting.tsx` (new)
- **How to verify:**
  - Run `bun run dev`
  - Call `createAccount({ clientId: '<uuid>', code: '1.1.01', name: 'Caja', type: 'asset' })` — inserts into accounting_account
  - Call `listAccounts({ clientId: '<uuid>' })` — returns accounts ordered by code
  - Call `createJournalEntry({ clientId: '<uuid>', entryDate: '2026-04-01', lines: [{ accountId: '<debitAccId>', debit: 1000, credit: 0 }, { accountId: '<creditAccId>', debit: 0, credit: 1000 }] })` — inserts entry + 2 lines
  - Try with unbalanced lines (debit ≠ credit) → throws "El asiento no está balanceado: ..."
  - Call `getTrialBalance({ clientId: '<uuid>', from: '2026-01-01', to: '2026-12-31' })` — returns all accounts with totals
- **Learnings for future iterations:**
  - Double-entry validation: use `Math.abs(totalDebit - totalCredit) > 0.001` tolerance to avoid floating-point issues
  - `leftJoin` in trial balance allows accounts with zero activity to appear (important for full chart of accounts view)
  - Account-per-line validation (checking each line's accountId belongs to the client) is O(n) queries — acceptable for typical journal entry sizes (2-20 lines)
  - `sql<string>\`COALESCE(SUM(...)::numeric, 0)\`` used in trial balance — returns string from PostgreSQL, parse with `parseFloat()` in UI
---

## Codebase Patterns
- Migration scripts live in `src/scripts/` and use `ADD COLUMN IF NOT EXISTS` via `db.execute(sql.raw(...))` for idempotent migrations
- Pre-existing TS errors exist in some `src/scripts/` files (map-import-empleados, report-unmatched, etc.) — these are not related to our changes
- TypeScript check: `bunx tsc --noEmit` (no `typecheck` script in package.json)
- Migration pattern requires `DATABASE_URL` env var and uses `dotenv/config` import

# Ralph Progress Log
Started: Fri Apr 24 16:44:39 -03 2026
---

## 2026-04-24 - US-003
- Added `updateProfileManagement` server function in `src/actions/profile.tsx` (POST, canWrite required): validates org ownership, sets `managedByStudy`, `disabledAt`, `disabledReason`
- Added `managedByStudy = true` filter to `listEmpleados` in `src/actions/sueldos.ts` to exclude unmanaged profiles from payroll module
- Updated "Perfiles Asociados" section in `src/components/client-detail-page.tsx`:
  - Profile badges wrapped in `div.relative.group` to show hover toggle button (EyeOff/Eye icon)
  - Unmanaged profiles rendered with 50% opacity and "No administrado" label
  - Selected profile detail footer shows "No administrado" badge when applicable
  - Toggle button calls `toggleProfileManagementMutation` with inverted `managedByStudy` value
- Files changed: `src/actions/profile.tsx`, `src/actions/sueldos.ts`, `src/components/client-detail-page.tsx`
- **How to verify:**
  - Run `bun run dev`, go to any client page
  - In "Perfiles Asociados" section, hover over a profile badge — a small eye-off icon appears
  - Click it: the profile turns grey/opaque and shows "No administrado"
  - Click the eye icon again to re-enable
  - Go to the Sueldos module for the same client — the disabled profile's employees should no longer appear in `listEmpleados`
- **Learnings for future iterations:**
  - The "perfiles tab" in client-detail-page is actually the "resumen" tab's "Perfiles Asociados" card (not a dedicated tab)
  - Large files like client-detail-page.tsx require Python script for replacements when indentation is tricky for the Edit tool
  - Pre-existing TS errors in credential.tsx, sueldos.ts(1665), and app-sidebar.tsx should be ignored (not introduced by our changes)
---

## 2026-04-24 - US-002
- Added `dataSourceEvent` table to `drizzle/schema.ts` with all required fields: id, organization_id (FK org), client_id (FK client nullable), profile_id (FK profile nullable), entity_type, entity_id, source, source_job_id (FK job nullable), action, metadata (jsonb nullable), created_at
- Created migration script `src/scripts/ensure-data-source-event-table.ts` using `CREATE TABLE IF NOT EXISTS` pattern
- Both files were already committed in `c24a998`; this entry marks the PRD as passing
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-data-source-event-table.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-data-source-event-table.ts` (should print "Tabla data_source_event: OK.")
  - Verify in DB: `\d data_source_event` should show all 10 columns
- **Learnings for future iterations:**
  - Pre-existing typecheck errors exist in `drizzle/relations.ts` (column naming camelCase mismatches) and `src/actions/credential.tsx` — these are not related to our changes
  - Always verify git log before re-implementing — the story may already be committed
---

## 2026-04-24 - US-004
- Added 7 columns to `notification` table in `drizzle/schema.ts`: severity (text default 'unclassified'), category (text nullable), ai_summary (text nullable), ai_classified_at (timestamp nullable), assigned_to_user_id (text FK user ON DELETE SET NULL), resolved_at (timestamp nullable), resolved_by_user_id (text FK user ON DELETE SET NULL)
- Added index `idx_notification_severity` on (client_id, severity)
- Created migration script `src/scripts/ensure-notification-enrichment-columns.ts`
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-notification-enrichment-columns.ts`
- **How to verify:**
  - Run: `timeout 60 bun run src/scripts/ensure-notification-enrichment-columns.ts` (should print "Columnas de enriquecimiento de notification: OK.")
  - Verify in DB: `\d notification` should show the 7 new columns and the new index
- **Learnings for future iterations:**
  - FK references to `user` table use `text` type (not uuid) — `text("field").references(() => user.id, { onDelete: "set null" })`
  - Index added inline in the pgTable second argument array alongside existing indexes
  - Pre-existing typecheck/lint errors in credential.tsx, sueldos.ts, relations.ts — not related to our changes
---

## 2026-04-24 - US-001
- Added `managed_by_study` (boolean default true), `disabled_at` (timestamp nullable), `disabled_reason` (text nullable), `profile_type` (text default 'unknown') columns to the `profile` table in `drizzle/schema.ts`
- Created migration script `src/scripts/ensure-profile-enrichment-columns.ts` following the pattern in `ensure-empleado-legajo-extra-columns.ts`
- Files changed: `drizzle/schema.ts`, `src/scripts/ensure-profile-enrichment-columns.ts`
- **How to verify:**
  - Run: `bun run src/scripts/ensure-profile-enrichment-columns.ts` (should print "Columnas de enriquecimiento de perfil: OK.")
  - Verify in DB: `\d profile` should show the 4 new columns
- **Learnings for future iterations:**
  - Migration scripts use `db.execute(sql.raw(stmt))` from drizzle-orm, importing `sql` and `db`
  - `dotenv/config` must be imported first in scripts
  - Pre-existing TS errors in unrelated script files should not block our work
---

## 2026-04-24 - US-032
- Created `src/actions/client-portal.tsx` with all required server functions:
  - `getClientPortalDashboard(clientId)`: returns client data, next 3 due dates, open debts (if permitted), unread notification count, pending requests, and permission flags
  - `getClientPortalDebts(clientId)`: returns full debt list (throws if `canViewDebts=false`)
  - `getClientPortalDueDates(clientId, includeCompleted?)`: returns due dates, optionally including completed ones
  - `getClientPortalNotifications(clientId, limit?)`: returns open (non-resolved) notifications
  - `getClientPortalRequests(clientId, status?)`: returns client requests, optionally filtered by status
  - `completeClientRequest(requestId)`: marks a request as completed (validates user access first)
- All functions call `getAuthSession()` and validate the calling user has a `clientUserAccess` row via internal `getClientPortalAccess()` helper
- Permission flags respected: `canViewDebts` gates debt queries in dashboard and `getClientPortalDebts`
- Files changed: `src/actions/client-portal.tsx` (new)
- **How to verify:**
  - Run `bun run dev`
  - Import and call `getClientPortalDashboard({ clientId })` from any component where the user has a `client_user_access` row — returns structured dashboard data
  - Call without a `client_user_access` row → throws "Acceso denegado al portal del cliente"
  - Call `getClientPortalDebts({ clientId })` with a user where `canViewDebts=false` → throws permission error
  - Call `completeClientRequest({ requestId })` → request status becomes 'completed' in DB
- **Learnings for future iterations:**
  - `getAuthSession()` (not `getSessionWithOrg()`) is the right helper for portal functions — portal users may not have an active org (they are client users)
  - `clientUserAccess` unique constraint is on `(clientId, userId)` — validate with `and(eq(userId), eq(clientId))`
  - `clientRequest.clientId` uses `clientId` naming (not `.client`) — consistent with `clientBalanceConfig.clientId` pattern for newer tables
  - `dueDate.client`, `debt.client`, `notification.client` are all `.client` (not `.clientId`) — older tables use this pattern
---

## 2026-04-24 - US-040
- Added `fechaAntiguedadReconocida` (timestamp nullable) column to `liquidacionImportEmpleado` in `drizzle/schema.ts`
- Created `src/scripts/ensure-fecha-antiguedad-reconocida.ts` migration script using ADD COLUMN IF NOT EXISTS pattern
- Ran migration script — output: "Columna fecha_antiguedad_reconocida en liquidacion_import_empleado: OK." (exit 124 expected)
- Updated seniority calculation in `src/actions/sueldos.ts`: now prefers `fechaAntiguedadReconocida` over `fechaAlta` when present (`fechaBaseAntiguedad = emp.fechaAntiguedadReconocida ?? emp.fechaAlta ?? periodoDate`)
- Files changed: `drizzle/schema.ts`, `src/actions/sueldos.ts`, `src/scripts/ensure-fecha-antiguedad-reconocida.ts` (new)
- **How to verify:**
  - Run: `docker exec arca-local-db psql -U arca -d arca -c '\d liquidacion_import_empleado'` — confirm `fecha_antiguedad_reconocida` column exists
  - Or: `timeout 60 bun run src/scripts/ensure-fecha-antiguedad-reconocida.ts` — prints "Columna fecha_antiguedad_reconocida en liquidacion_import_empleado: OK."
  - For seniority: set `fecha_antiguedad_reconocida` on an employee with a different `fecha_alta` and verify the payroll preview uses the recognized date
- **Learnings for future iterations:**
  - All pre-existing typecheck errors are in legacy scripts — not regressions from our changes
---
